name: developer_tools_local_auth
description: "Local Auth integration for developer_tools. Check biometric support, view enrolled biometrics, test authentication, and copy auth status from the debug overlay."
version: 0.0.1
topics: ["development", "debug", "tools", "local-auth", "flutter"]

repository: https://github.com/ramz/developer_tools
issue_tracker: https://github.com/ramz/developer_tools/issues
homepage: https://github.com/ramz/developer_tools/tree/main/packages/developer_tools_local_auth
license: MIT

resolution: workspace

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  developer_tools_core: ^0.0.3

  local_auth: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
