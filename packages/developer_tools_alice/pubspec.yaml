name: developer_tools_alice
description: "Alice HTTP Inspector integration for developer_tools. Open the HTTP inspector and view status from the debug overlay."
version: 0.0.2
topics: ["development", "debug", "network", "alice", "flutter"]

repository: https://github.com/ramz/developer_tools
issue_tracker: https://github.com/ramz/developer_tools/issues
homepage: https://github.com/ramz/developer_tools/tree/main/packages/developer_tools_alice
license: MIT

resolution: workspace

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  developer_tools_core: ^0.0.4

  alice: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
